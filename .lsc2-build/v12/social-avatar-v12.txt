function SocialAvatarContent({name,src}:{name:string;src?:string|null}) {
  return <>
    {src ? <img src={src} alt="" onError={(event) => event.currentTarget.remove()} /> : null}
    <span className="avatar-fallback">{initials(name)}</span>
  </>
}
