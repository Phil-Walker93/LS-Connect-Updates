async function v076LoadMyTickets(){if(state.mode!=='online'||!state.activeCharacterId)return[];const{data,error}=await db.rpc('my_feedback_tickets',{p_character_id:state.activeCharacterId});if(error){throwWithToast(error);return[];}return data||[];}
async function openFeedbackCenterV076(){const tickets=await v076LoadMyTickets();openModal('Feedback & Tickets',`<section class="settings-block"><h3>Neues Ticket</h3><form id="feedbackTicketForm" class="stack-form"><label>Kategorie<select id="ticketCategory"><option value="bug">Bug melden</option><option value="change">\u00c4nderungswunsch</option><option value="suggestion">Vorschlag</option></select></label><label>Titel<input id="ticketTitle" maxlength="120" required placeholder="Kurze Zusammenfassung"></label><label>Beschreibung<textarea id="ticketDescription" maxlength="4000" rows="6" required placeholder="Beschreibe den Fehler oder Wunsch m\u00f6glichst genau."></textarea></label><button id="submitTicket" class="primary-button" type="submit">Ticket senden</button></form></section><section class="settings-block"><h3>Meine Tickets</h3><div class="ticket-list">${tickets.length?tickets.map(t=>`<article class="ticket-card status-${t.status}"><div class="ticket-card-head"><strong>${escapeHtml(t.title)}</strong><span>${escapeHtml(v076StatusLabel(t.status))}</span></div><small>${escapeHtml(v076CategoryLabel(t.category))} \u00b7 ${escapeHtml(v076FormatDate(t.created_at))} \u00b7 Priorit\u00e4t ${escapeHtml(v076PriorityLabel(t.priority))}</small><p>${escapeHtml(t.description)}</p>${t.admin_note?`<div class="ticket-admin-note"><b>Admin-Notiz</b>${escapeHtml(t.admin_note)}</div>`:''}</article>`).join(''):'<p class="notification-note">Noch keine Tickets f\u00fcr diesen Charakter.</p>'}</div></section>`);$('feedbackTicketForm')?.addEventListener('submit',async e=>{e.preventDefault();const btn=$('submitTicket');setBusy(btn,true,'Sende\u2026');try{const{error}=await db.rpc('create_feedback_ticket',{p_character_id:state.activeCharacterId,p_category:$('ticketCategory').value,p_title:$('ticketTitle').value.trim(),p_description:$('ticketDescription').value.trim()});if(error)throw error;showToast('Ticket wurde gesendet.','success');await openFeedbackCenterV076();}catch(err){throwWithToast(err);}finally{setBusy(btn,false);}});}

const v076AccountBase=openAccountModal;
openAccountModal=async function openAccountModalV076(){await v076AccountBase();if(!els.modalContent)return;if(!$('feedbackCenterButtonV076')){const block=document.createElement('div');block.className='settings-block';block.innerHTML='<h3>Feedback & Tickets</h3><p class="notification-note">Melde Bugs oder schicke \u00c4nderungs- und Verbesserungsvorschl\u00e4ge direkt an die Administration.</p><button id="feedbackCenterButtonV076" class="ghost-button wide" type="button">Tickets \u00f6ffnen</button>';const info=els.modalContent.querySelector('.whats-new-block');if(info)els.modalContent.insertBefore(block,info);else els.modalContent.appendChild(block);$('feedbackCenterButtonV076')?.addEventListener('click',openFeedbackCenterV076);}};
